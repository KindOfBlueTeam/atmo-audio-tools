services:
  - type: web
    name: midi-analysis
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "midi_analysis.web:create_app()" --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
